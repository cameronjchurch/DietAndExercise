{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "DietAndExerciseRepo": "/data",
  // Sample configuration keys (Development)
  "ConnectionStrings": {
    // Replace with your Postgres connection string, e.g. "Host=localhost;Database=diet;Username=app;Password=pass"
    "DietAndExercise": ""
  },
  // Toggle database-backed services. If true the app will attempt to use EF/Npgsql (requires a valid ConnectionStrings:DietAndExercise).
  "UseDatabase": true
}
